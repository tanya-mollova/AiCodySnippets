{
  "name": "aicodysnippets",
  "version": "1.0.0",
  "description": "A code snippets sharing application built with MERN stack",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["code-snippets", "mern", "react", "express", "mongodb"],
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
